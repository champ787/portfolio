import React, { useEffect, useRef } from 'react';
import './App.css';
import { RoutesFunc } from './Routes';


function App() {
  const imageRef = useRef(null);
  
  return (
    
      <RoutesFunc/>

  );
}

export default App;